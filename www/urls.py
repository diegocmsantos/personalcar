from django.conf.urls.defaults import patterns, include, url
from django.contrib.auth import views

urlpatterns = patterns('www',
    # client
    # (r'^add_client/$', 'views.add_client'),
    # (r'^list_client/$', 'views.list_client'),
)
